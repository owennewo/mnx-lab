{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mnx-lab.totai.uk/schemas/scenario-meta.schema.json/v1",
  "title": "MNX Lab scenario metadata (meta.json)",
  "description": "Sidecar metadata for one scenario in the corpus. Identity (id, category) is derived from the folder path and deliberately NOT stored here. See roadmap/inprogress/04-scenario-library.md.",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "minLength": 1
    },
    "description": {
      "type": "string",
      "minLength": 1
    },
    "bars": {
      "type": "integer",
      "minimum": 1
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9:-]+$"
      },
      "uniqueItems": true
    },
    "specRefs": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uri"
      },
      "description": "Pointers into the MNX spec (mnx-reference/objects/... pages)."
    },
    "coversDefs": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "description": "Schema $defs this scenario exercises \u2014 drives the coverage report."
    },
    "schema": {
      "enum": [
        "published",
        "proposed"
      ],
      "description": "Which MNX schema `expect.standard` is judged against. Default \"published\" \u2014 the vendored spec release. \"proposed\" opts into schemas/mnx-schema.proposed.json, a spec change drafted in our fork but not adopted by the CG; use it so a proposal can be proved against the corpus instead of only described. See docs/mnx-spec-submodule.md."
    },
    "expect": {
      "type": "object",
      "properties": {
        "standard": {
          "enum": [
            "valid",
            "invalid"
          ]
        },
        "extension": {
          "enum": [
            "valid",
            "invalid",
            "n/a"
          ],
          "description": "Verdict against the _x.mnxLab extension schema; n/a when the document carries no _x.mnxLab."
        },
        "errors": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Required iff any verdict is 'invalid': fragments that must each match at least one actual validation error."
        }
      },
      "required": [
        "standard",
        "extension"
      ],
      "additionalProperties": false
    },
    "requires": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "description": "Renderer capabilities exercised (free-form for now)."
    },
    "idRefs": {
      "type": "boolean",
      "description": "Does the scenario exercise id-based cross-referencing (ties, slurs, technique targets)?"
    },
    "source": {
      "type": "string",
      "pattern": "^(hand-written|spec-example|llm|converter:.+)$"
    },
    "status": {
      "enum": [
        "draft",
        "valid",
        "rendered",
        "verified"
      ]
    }
  },
  "required": [
    "title",
    "description",
    "expect",
    "source",
    "status"
  ],
  "additionalProperties": false
}
