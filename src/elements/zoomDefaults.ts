/** Product defaults shared by Studio, the workbench, and every reset route. */
export const DEFAULT_STAFF_SP = 1;
export const DEFAULT_SPACE_SP = 2;
export const DEFAULT_SPACING_MODE = 'fill' as const;
